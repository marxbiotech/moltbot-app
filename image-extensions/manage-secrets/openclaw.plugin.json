{
  "id": "manage-secrets",
  "name": "Manage Secrets",
  "description": "Manage agent secrets and configuration. Provides set_secret (environment secrets) and set_config (runtime config) tools.",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
