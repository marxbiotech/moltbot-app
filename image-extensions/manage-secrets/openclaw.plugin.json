{
  "id": "manage-secrets",
  "name": "Manage Secrets",
  "description": "GitOps mutation tools: set environment secrets (set_secret) and update runtime config (set_config) via GitHub Actions workflow dispatch.",
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
